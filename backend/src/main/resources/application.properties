spring.application.name=turismoweb

# PostgreSQL Cloud Configuration (Neon)
spring.datasource.url=jdbc:postgresql://ep-little-feather-apap9nzv.c-7.us-east-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=neondb_owner
spring.datasource.password=npg_Gx07SkLINMvP

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# OAuth2 Resource Server Configuration (Google)
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://accounts.google.com
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=https://www.googleapis.com/oauth2/v3/certs

# Frontend URL and CORS Configuration
frontend.url=${FRONTEND_URL:http://localhost:5173}
cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173}

# OpenAI Configuration for Local Development
spring.ai.openai.api-key=dummy-key-local
spring.ai.openai.chat.enabled=false